# thinking リポジトリのエージェント規約

このファイルは、`thinking` リポジトリで作業する Codex、Claude Code、opencode 共通の規約です。ユーザーが明示した依頼を優先しつつ、ここに書かれた品質基準を守ります。

## リポジトリの前提

- `docs/` がGitHub Pagesの公開対象です。
- ページは単一HTMLファイルを基本とし、CSSとJavaScriptはページ内に置きます。
- 記事の一覧は `docs/index.html` に登録します。
- 公開経路は `.github/workflows/pages.yml` です。Pages設定の変更やpushは、明示的な依頼なしに実行しません。

## 作業フロー

作業は次のフェーズ境界を会話と状態ファイルに明示します。

1. **Research**：リポジトリの事実、既存ページ、設定、未完了の変更を調査する。
2. **Plan**：目的、成功条件、対象読者、変更範囲、検証方法をユーザーと確定する。
3. **Implement**：計画を確定してから、専用worktreeで変更する。
4. **Review**：差分、テスト、表示、アクセシビリティ、公開導線を確認する。

長期作業では `agent-workflow resolve` を実装前に1回実行し、カードがない場合はローカル状態を使います。フェーズ境界・完了時には `agent-workflow capture` を実行します。作業状態の正本は、コマンドが返すstateDirectory内の日本語5ファイルです。

新規作業は別worktreeと英数字のブランチで行います。ユーザーの無関係な変更を保持し、タスクに必要なファイルだけを変更・commitします。commit前に宣言済みのlint、単体テスト、ページ検証を実行します。

振る舞いを変更する場合はRED → GREEN → REFACTORで進めます。新しい実行可能コードを追加する場合は、カバレッジ98%以上を満たせるテストを同時に追加します。今回の公開検証はworkflow内の標準コマンドに置き、独立した検査スクリプトは増やしません。

## 記事作成の品質基準

### 読者と方向性

記事の作成・改稿を始める前に、次をユーザーへ確認します。

- 想定読者
- ページの目的と読後に期待する行動
- 扱う範囲、深さ、主張の方向性
- 不明な用語、前提、事実、優先順位
- 図・表・文章のどれを中心にするか迷う場合の選択

リポジトリから確認できる事実は先に調査し、ユーザーの意図や方向性だけを質問します。重要な不明点が残る場合は推測で本文を作らず、確認が完了するまで止まります。省略した結果わからなくなる説明は省略せず、重複や不要な修飾だけを推敲して短くします。

### 図表とdiagram-design

- 数値・項目・役割の比較は、まず表で表現します。
- 関係、工程、状態遷移、責務分担、因果関係は、図にすると理解が速くなる場合に図解します。
- 単純な一覧や一文で伝わる内容に図を追加しません。
- 図には必ず本文による読み方・前提・限界の説明を添えます。色や位置だけに意味を持たせません。
- 図を作るときは `diagram-design` の意味パターンと型リファレンスを読み、対象読者・サイズ・詳細度を先に決めます。
- diagram-designのスタイルガイド確認、CJKフォント、`role="img"`、`title`、`desc`、キーボードやモバイルでの可読性を守ります。
- ノードや線を増やす前に削除・統合を検討し、複雑な図は概要と詳細へ分割します。

### HTMLとアクセシビリティ

- `<!DOCTYPE html>`、`<html lang="ja">`、viewport、title、descriptionを用意します。
- 見出し階層、表の見出し、リンクの目的、フォームやタブの状態を意味的に表します。
- JavaScriptが無効でも主要な説明を読める構成にします。
- ローカルリンクは相対パスで記述し、記事フッターから適切な階層のトップへ戻れるようにします。
- 外部フォントや外部サービスを追加する場合は、必要性と公開環境での失敗時の見え方を確認します。

## 検証と公開

- `git diff --check` を実行します。
- `docs/index.html`、`docs/.nojekyll`、全HTMLの必須要素、ファイル名、相対リンク、アンカーを確認します。
- `python3 -m http.server -d docs 8000` でトップと記事を確認します。
- デスクトップ幅・モバイル幅、キーボード操作、ダークモード、`prefers-reduced-motion` を確認します。
- 秘密情報、ローカル専用URL、未解決のTODO、壊れた公開URLを残しません。
- GitHub Pages設定の読み取りは許可しますが、設定変更、commit、push、公開後の外部確認は明示的な依頼なしに行いません。

完了時は、変更ファイル、検証結果、残っている手動作業、次にできるタスク候補を日本語で報告します。
